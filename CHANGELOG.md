# 更新日志

## v0.2.1 (2026-08-08)

自 v0.1.0 以来的一次大版本更新,数值计算能力全面扩展:

### 新增:复数运算(完整复数管线)

- 虚数单位 `i`:`2i`、`(1+2i)*(3-4i)`、`e^(iπ)` = -1、`sqrt(-1)` = i、`ln(-1)` = iπ
- 全部内置函数支持复数(主分支约定):`sin` `cos` `tan` `asin` `acos` `atan` `sinh` `cosh` `tanh` `asinh` `acosh` `atanh` `sqrt` `exp` `ln` `log` `log2` `log10` `logb`
- 复数分量/共轭/辐角:`re(z)` `im(z)` `conj(z)` `arg(z)`;`abs(z)` 为模
- 结果自动显示 `a+bi`(虚部为 0 时仍显示实数);DEG 角度模式同样支持复数
- 复参数下的精确矫正保留:`e^(iπ)` 精确显示 -1、`tan(π/2)` 仍为 ∞

### 新增:自定义函数

- 主窗口底部函数输入框,自动命名 `f_1(x)`、`f_2(x)`…,保存后可在表达式/函数体/Σ/∫/deriv 中直接调用
- 函数体支持任意表达式组合与函数互相调用;自引用/循环引用有深度保护
- 输入 `f` 时自动弹出函数下拉列表(表达式框与函数体框均可),`↑`/`↓` 选择、`Enter` 插入
- F2 设置页新增"自定义函数"标签(←/→ 切换),支持查看/内联编辑/删除
- 删除被其他函数引用的函数会被拦截并提示全部直接引用者;删除时同步清理历史中相关记录
- 函数持久化到配置文件,重启后保留;删除不重编号,新函数复用最小空闲编号

### 新增:无穷级数与瑕积分

- `∞` 符号直接键入(或 `-∞`)
- 收敛级数:`Σ(k,1,∞,1/k²)` ≈ 1.6449;发散级数明确报"未收敛"
- 瑕积分:`∫(x,1,∞,1/x²)` = 1;积分区间采用环带累加,宽区间也能高效收敛

### 新增:数值函数与常量

- 组合/排列数 `C(n,k)` `A(n,k)`(乘法公式,大数不中间溢出)
- Γ 函数 `gamma(x)`(实数阶乘推广,Lanczos 近似)
- 余函数 `sec` `csc` `cot` `sech` `csch` `coth`(支持复数)
- 统计函数(变参):`mean(x,…)` 均值、`var(x,…)` 总体方差、`std(x,…)` 标准差,以及 `max`/`min`
- `frac(x)` 小数部分、`round(x,n)` 指定位数舍入
- 常量 `ln2` `ln10` `sqrt2` `γ`(欧拉常数,直接键入 γ)

### 改进

- 表达式解析引擎重构:以自研分词 + 调度场算法替换已停维护的 meval,消除其 10 年前 nom 依赖的未来兼容性警告,零第三方解析依赖
- 三角函数浮点误差精确矫正:全周期扫描保证 sin/cos/tan 在 π/2 整数倍处返回精确 0/±1/±∞,`sin(180°)` 显示 0 而非 1.22e-16
- 正负无穷正确区分显示(`tan(-π/2)` = -∞);`-0` 归一显示为 0
- 预设组合按键(π/Σ/∫/deriv/√/φ,键盘无法直接键入)标注"固定",可修改按键但不能删除
- DEG/RAD 徽标移至表达式输入框最右端,输入状态一目了然
- 移除鼠标捕获,恢复终端原生框选复制;焦点切换由 `Ctrl+F` 承担
- kitty 终端键盘协议适配:Ctrl+3 不再与 Esc 混淆

### 修复

- 组合按键在字符串中间输入时误删后续字符(`sin()*2*cos(π)` 中插入 `\p` 出错)
- 反三角函数在复数域的主分支取值(`acos(2)` 与 `π/2-asin(2)` 恒等式在复数域不成立)
- `sqrt(-1)` 因 `-0.0` 虚部导致的辐角方向错误
